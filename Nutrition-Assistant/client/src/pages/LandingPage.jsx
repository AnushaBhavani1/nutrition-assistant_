// pages/LandingPage.jsx
// SmartBridge spec requires a page file named "LandingPage.jsx". The
// existing `Landing.jsx` already implements the public marketing page and
// is mounted at "/" — this re-exports it so both names resolve to the same,
// single implementation (no duplicated code).

export { default } from './Landing';
