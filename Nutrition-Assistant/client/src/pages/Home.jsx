// pages/Home.jsx
// SmartBridge spec requires a page file named "Home.jsx" for the
// authenticated landing view. `Dashboard.jsx` already implements this
// (welcome message, today's stats, BMI, goal, recent meals, weekly
// progress) — re-exported here so both names resolve to the same page.

export { default } from './Dashboard';
